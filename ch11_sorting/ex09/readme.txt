习题9
编写程序，在O(n)时间内从数组x[0..n-1]中找出第k个最小的元素。
算法可用对x中的元素进行排序。

分析：
可以考虑的方法如
a “插入排序”，对于最大或者最小值，时间范围在0(n);其余的值大概要花O（n2）;
b “位图法”（第一章的算法），对于没有重复的值或者了解最大重复的次数的情况下，也可以考虑此算法；
c “快速排序法”，是个比较好的方法，代码就是利用这个算法找到第k个最小的元素。
